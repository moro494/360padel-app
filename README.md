# 360 Padel — App organizzazione partite

Sito statico (GitHub Pages) generato automaticamente da `~/360padel/aggiorna.py`.

L'HTML reale è in `c7fd7f9e/organizzazione_partite.html` (URL non in elenco).
